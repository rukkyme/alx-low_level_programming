#include "main.h"
#include <stdio.h>

/**
 * abs - function that computes the absolute value of an integer
 * @c: this input is an integer
 * Return: 0
 */
int _abs(int)
{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else
	return ( c * -1);
}
