#include "main.h"
/**
 * _isupper -this checks if the letter is uppercase or not
 *@c: print int
 * Return: Always 0.
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}

	else
	{
	return (0);
	}
}

